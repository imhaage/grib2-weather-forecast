node_modules/
package-lock.json
apps/visualize/dist/
.DS_Store

# External libraries (sources only, WASM already compiled)
eccodes/
libaec/

# Git worktrees
.worktrees/

# Decoder build output — committed until npm publish is set up
!packages/grib2-decoder/dist/
