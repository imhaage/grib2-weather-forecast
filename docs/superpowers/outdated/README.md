# Outdated Docs

This directory stores historical plans and specs that are no longer active.

They are kept for context and decision history, but they should not be treated as the current implementation backlog.
